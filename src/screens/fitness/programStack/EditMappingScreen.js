import React from "react";
import {View} from "react-native-animatable";
import {TextInput} from "react-native-paper";

const EditMappingScreen = ({navigation}) => {


  const {data} = this.props;

  const renderRow = () => {
    if (data.setRepMap !== 'undefined') {

    }
  }
  return (
    <View>
    </View>
  );
}

